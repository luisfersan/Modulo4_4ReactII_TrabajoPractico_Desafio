import { createTheme } from '@mui/material/styles';

export const theme = createTheme( {
  palette: {
    type: 'light',
    primary: {
      main: '#388e3c',
    },
    secondary: {
      main: '#d50000',
    },
    background: {
      default: '#eeeeee',
    },
  },
});